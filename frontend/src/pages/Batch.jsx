import PlaceholderPage from '@/components/PlaceholderPage';
import { Layers } from 'lucide-react';
export default function BatchPage() {
  return <PlaceholderPage title="Batch Operations" description="Run bulk operations on student records, grades, and notifications." icon={Layers} />;
}
