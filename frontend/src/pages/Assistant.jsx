import PlaceholderPage from '@/components/PlaceholderPage';
import { Bot } from 'lucide-react';
export default function AssistantPage() {
  return <PlaceholderPage title="AI Assistant" description="Ask anything — powered by AWS Bedrock + Claude. Your 24/7 campus guide." icon={Bot} />;
}
