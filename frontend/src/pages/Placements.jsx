import PlaceholderPage from '@/components/PlaceholderPage';
import { Briefcase } from 'lucide-react';
export default function PlacementsPage() {
  return <PlaceholderPage title="Placements" description="Browse job listings, track applications, and prepare with AI mock interviews." icon={Briefcase} />;
}
