import PlaceholderPage from '@/components/PlaceholderPage';
import { CalendarDays } from 'lucide-react';
export default function CalendarPage() {
  return <PlaceholderPage title="Calendar" description="See all deadlines, events, and holidays in one unified calendar." icon={CalendarDays} />;
}
